import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-client',
  imports: [RouterLink],
  templateUrl: './client.html',
  styleUrl: './client.css',
})
export class Client {

}
